<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.work.domain.user.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.work.domain.user.entity.User">
        <id     property="id"       column="id"/>
        <result property="email"    column="email"/>
        <result property="password" column="password"/>
        <result property="role"     column="role"/>
    </resultMap>


    <select id="findByEmail" parameterType="string" resultMap="userResultMap">
        SELECT
            id,
            email,
            password,
            role
        FROM work_users
        WHERE email = #{email}
          AND deleted_at IS NULL
    </select>


    <insert id="insertUser" parameterType="com.work.domain.user.entity.User">

        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT work_users_seq.NEXTVAL FROM dual
        </selectKey>

        INSERT INTO work_users (
        id,
        email,
        password,
        role,
        created_at,
        updated_at
        )
        VALUES (
        #{id},
        #{email},
        #{password},
        <choose>
            <when test="role != null">
                #{role}
            </when>
            <otherwise>
                'USER'
            </otherwise>
        </choose>,
        SYSTIMESTAMP,
        SYSTIMESTAMP
        )
    </insert>

</mapper>